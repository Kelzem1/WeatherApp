let nublado = 'https://www.pexels.com/es-es/foto/nubes-cumulos-209831/'

const ImageChanger = ({weather}) =>{
    if (weather.weather[0].description) = 'muy nuboso' ? {
        {nublado}
    }
    
    return(

    )
}