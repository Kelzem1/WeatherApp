const Cards =({title, description, image})=>{
    return(
        <div className="">
            <h1>{title}</h1>
            <h4>{description}</h4>
            <span>{image}</span>
        </div>
    )
}
export default Cards